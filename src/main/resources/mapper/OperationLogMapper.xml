<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.mapper.OperationLogMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_operation_log (user_id, type, content) VALUES (#{userId}, #{type}, #{content})
    </insert>
    <select id="findByUserId" resultType="com.campus.entity.OperationLog">
        SELECT * FROM t_operation_log WHERE user_id = #{userId} ORDER BY create_time DESC LIMIT 200
    </select>
    <select id="findAll" resultType="com.campus.entity.OperationLog">
        SELECT * FROM t_operation_log ORDER BY create_time DESC LIMIT #{limit}
    </select>
</mapper>
